def sum(a,b)
{
  echo"The sum of ${a} and ${b} is ${a+b}"
}

def sub(a,b)
{
   echo"The subraction of ${a} and ${b} is ${a-b}"
}

def mul(a,b)
{
   echo"The multiplication of ${a} and ${b} is ${a*b}"
}

def div(a,b)
{
   echo"The division 0f ${a} and ${b} is ${a/b}"
}
